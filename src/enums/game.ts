export enum Difficulty {
    STARTER = 0,
    EASY = 1,
    MEDIUM = 2,
    HARD = 3,
    EXPERT = 4,
    LEGEND = 5,
    ULTIMATE = 6
}

export enum State {
    NOTSTARTED = 0,
    READY = 1,
    UNKNOWN_ERROR = 2,
    INVALID_BOOK = 3,
    FINISHED = 4
}